export type CreateNiveau = {
    titre_niveau: string, 
    descri_niveau: string, 
    domaine: string, 
    mention: string, 
    parcours: string
}

export type EditNiveau = {
    id_niveau: number, 
    titre_niveau: string, 
    descri_niveau: string, 
    domaine: string, 
    mention: string, 
    parcours: string
}